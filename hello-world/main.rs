// The main function is the entrypoint for every rust program
fn main() {
    // The default indent is 4-space, not tab.
    // The println! is a macro, not a function.
    println!("Hello, world!")
}

// Compiling and running are two different steps.
// To compile, use `rustc main.rs`, to run, use `./main`